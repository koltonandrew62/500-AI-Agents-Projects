"""HTTP + WebSocket API surface for J.A.R.V.I.S.

Owned entirely by agent `be-api` (see docs/CONTRACTS.md section 2). Nothing
outside `app/main.py` and `app/api/**` should be touched by this package.
"""

from __future__ import annotations
